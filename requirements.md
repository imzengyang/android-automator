# 项目需求文档

## 1. 背景
本项目计划开发一款基于 Electron 的桌面应用，用于连接 Android 手机并获取系统相关信息。应用将使用 Node.js 开发，内部通过调用 `adb` 命令来实现与设备的交互。

## 2. 功能需求
1. **设备连接与管理**
   - 自动检测并列出连接的 Android 设备。
   - 支持通过 USB 或无线方式连接设备。
   - 显示设备的基本信息（型号、系统版本等）。
2. **系统信息获取**
   - 利用 `adb` 命令获取设备的 CPU、内存、存储等硬件信息。
   - 获取系统配置、网络状态及已安装应用列表等。
   - 提供必要的刷新、重载功能。
3. **用户界面**
   - 简洁直观的主界面，展示设备列表与关键指标。
   - 提供日志窗口或面板，显示 `adb` 输出和错误信息。
   - 支持多语言（至少包括中文和英文）。
4. **稳定性与错误处理**
   - 对常见错误（设备未连接、权限不足等）进行提示与处理。
   - 记录应用日志，便于排查问题。

## 3. 技术栈
- **Electron**：实现跨平台桌面应用。
- **TypeScript**：提供强类型支持。
- **React**：用于构建用户界面。
- **Ant Design**：常用 UI 组件库。
- **Node.js**：业务逻辑和 `adb` 调用。
- **adb**：与 Android 设备通信。

## 4. 运行与安装
1. 安装 Node.js 环境。
2. 安装 Android SDK，并确保 `adb` 命令在系统路径下可用。
3. 克隆项目并安装依赖（如 `npm install`）。
4. 运行应用（如 `npm start` 或 `electron .`）。

## 5. 后续计划
- 增加更多设备操作功能，如安装/卸载应用、截屏录屏等。
- 与 Android Debug Bridge（ADB）更多特性集成，如日志抓取、Shell 命令执行等。

